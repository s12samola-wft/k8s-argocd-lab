apiVersion: v1
kind: Service

metadata:
  name: nginx-practice-service
  namespace: argocd-practice

spec:
  selector:
    app: nginx-practice

  ports:
    - port: 80
      targetPort: 80

  type: ClusterIP